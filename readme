Connect to posgresql
psql -U postgres

Connect to db
\c ispend_db

Show all dbs
\l

Show tables in current db
\d

Start postgresql container with volume
docker run --name some-postgres -e POSTGRES_PASSWORD=mysecretpassword -d -p 5432:5432 -v postgresql_data:/var/lib/postgresql postgres
