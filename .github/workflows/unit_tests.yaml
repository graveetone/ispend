name: 🧪 Unit tests

on: push
jobs:
  unittests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install requirements.txt
        run:
          pip install -r requirements.txt

      - name: Run unit tests
        run: |
          pytest -vs tests --random-order --random-order-bucket=global
